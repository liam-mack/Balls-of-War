import React from "react";

function GameHand(props) {
  console.log(props);
  return (
  // {Object.keys(props).map(props[element] => (<h1>{element}: {props[element]}</h1>)}
    <h1>RER</h1>
  );
}

export default GameHand;

// <h1> name : value </h1>

// Object.keys(result).map((item, i) => (
//   <div key={i} className="report">
//           <h3>{result[item].name}</h3>
